# Bash Exercise

This script go throw each file in a given folder.
For each file it adds a new line with the file name (including extensions).

## Output

New file inside `/usr/edited_file_list` that stored all the file names that was edited.
The new file name consist of the format: `dd_mm_yyyy-hh_mm_ss`
